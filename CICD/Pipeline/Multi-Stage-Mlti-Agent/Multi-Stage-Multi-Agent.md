# Multi-Stage Multi-Agent

#### Set up a multi-stage Jenkins pipeline where each stage is run on a unique agent. This is a very useful approach when you have a multi-language application or application that has conflicting dependencies.
